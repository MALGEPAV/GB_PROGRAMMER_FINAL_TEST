LINUX:

При помощи dpkg установил deb пакет, который добавляет репозиторий от mysql (deb - пакет скачал с их сайта через wget).
Установил workbench через apt.
Удалил workbench через dpkg.
История команд в txt - файле, очень длинные выводы команд обрезаны.

MySql:

Не совсем понял задания, номер 12 вообще не понял,
сделал как понял

Java:
В задании говорится только про Домашних животных, поэтому ослов,верблюдов и лошадей нет.
Есть родительский абстрактный класс Pet, от которого унаследованы Cat, Dog, Hamster
Можно добавлять животных, если попытка добавить животное без имени, бросается исключение,
без команд можно.
При успешном добавлении счетчик животных увеличивается на 1.
Про try with resources не понял задание.

Можно добавлять команды дивотному, для этого надо ввести ID животного, который можно узнать, 
если вывести всех животных на экран.

